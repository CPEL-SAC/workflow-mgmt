name: runner-smoketest
on:
  push:
    branches: [ci/runner-smoketest]
  workflow_dispatch:
jobs:
  selfhosted:
    runs-on: oxiacore-gke-runner
    steps:
      - run: echo "self-hosted OK en $(hostname)"
